============
Installation
============

At the command line::

    $ easy_install orientdbcli

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv orientdbcli
    $ pip install orientdbcli
